# Method Comparison

ASTRA is meant to become a method-comparison surface, not just a place to host one preferred solver.

For that reason, every later formulation comparison should be written against explicit criteria:

- convergence behavior,
- conditioning,
- robustness,
- timestep behavior,
- physical diagnostics,
- implementation complexity.

That comparison framework only becomes meaningful once the classical baseline is real.
