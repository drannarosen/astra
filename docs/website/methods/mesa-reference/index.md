# MESA Reference

This subtree will hold file-backed comparison notes against the local MESA source mirror. The goal is to document only the solver, scaling, boundary, and variable-layout behaviors we can verify directly in source.
