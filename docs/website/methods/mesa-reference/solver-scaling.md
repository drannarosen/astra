# Solver Scaling in MESA

This page will summarize the MESA source-backed scaling patterns that matter for ASTRA, especially the correction weights and state scaling used during the solve. It will stay anchored to the local mirror rather than memory.
