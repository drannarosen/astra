# ASTRA Benchmark Placeholders

This directory exists to hold the first performance artifacts once ASTRA's classical baseline becomes meaningful.

At bootstrap, the benchmark script only measures:

- toy residual assembly,
- toy Jacobian construction,
- toy nonlinear solve runtime.

Those numbers are useful for regression tracking, but they are not yet scientific performance claims about a real stellar solver.
